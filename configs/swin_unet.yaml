model: "swin_unet"
in_channels: 3
n_classes: 1

img_size: 448

# SwinUNETR hyperparams
feature_size: 48
depths: [2, 2, 6, 2]
num_heads: [3, 6, 12, 24]
drop_rate: 0.0
attn_drop_rate: 0.0
dropout_path_rate: 0.1
use_checkpoint: false
